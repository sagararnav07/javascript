1. children function : use to access children of the class

--> const parent = document.querySelector('.parent')
-->  console.log(parent);
-->  console.log(parent.children[1].innerHTML);

------------------------------------------------------------------
2 .Traversing the cildren of the class using for loop

--> for (let i = 0; i < parent.children.length; i++) {
    console.log(parent.children[i].innerText)
    }

------------------------------------------------------------------
3 .firstElementChild / .lastElementChild : returns 1st or last element of parent class

--> console.log(parent.firstElementChild)
--> console.log(parent.lastElementChild)

------------------------------------------------------------------
4 .parentElement : return parent class from child class

--> console. log (dayOne);
--> console. log(dayOne.parentElement);
--> console.log(dayOne.nextElementSibling); //returns next consecutive children

------------------------------------------------------------------
5 .childNodes : (important timestamp 16:42) so it says complete DOM is a tree structure and even 
                spacebreaks takes some space, "NodeList" is discussed intensively
                Memory manipulation in react is done via child nodes in open source

--> console.log('NODES: ', parent.childNodes);


​
